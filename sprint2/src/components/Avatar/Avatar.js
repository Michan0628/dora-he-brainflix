import React from "react";
import "./style.scss";

export default function Avatar(props) {
  return (
    <figure className={props.className + " avatar"}>
      {/* USER PROFILE */}
    </figure>
  );
}
